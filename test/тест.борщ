// вхідне_число = ціле(ввід("Введіть число: "));

10; 10;

ф = 5;
друкр(6.0 - --дійсне(ф));

// якщо((вхідне_число >= 5) і (вхідне_число <= 10))
10; якщо(вхідне_число > 10)
{
    друкр("Якщо");
}
інакше якщо(вхідне_число - 5)
{
    друкр("Інакше якщо");
}
інакше
{
    друкр("Інакше");
}
