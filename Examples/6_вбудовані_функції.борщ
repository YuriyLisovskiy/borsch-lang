/*              Документація | Вбудовані функції
 *
 *   Мова Борщ пропонує вбудовані функції для допомоги при
 *   написанні коду.
 */

/*   1. Введення/Виведення
 *
 *   Мова Борщ пропонує вбудовані функції для допомоги при
 *   написанні коду.
 *
 *   Для введення даних потрібно використати вбудовану функцію
 *   ввід(), яка ніде не реалізована мовою Борщ. Функція приймає
 *   рядок, який виведе для повідомлення, що саме потрібно ввести,
 *   і повертає рядок.
 *
 *   Вивід даних відбувається за допомогою вбудованої функції
 *   друк() та друкр() для друку об'єкта без переносу на новий
 *   рядок та з ним відповідно. Друкувати можна вбудовані типи:
 *   цілий, дійсний та рядок.
 */

// Приклад введення даних з терміналу.
дійсне_число = дійсний(ввід("Введіть дійсне число: "));
ціле_число = цілий(ввід("Введіть ціле число: "));
рядкове_значення = ввід("Введіть текст: ");
логічна_змінна = логічний(ввід("Введіть логічне значення: "));

// Приклад виведення даних в тремінал.
друк("Цей текст був надрукований ");
друкр(1, "червня", 2021, "року.");


/*   2. Конструктори стандартних типів
 *
 *   Перетворення типів відбувається за допомогою вбудованих
 *   функцій, які ніде не реалізовані мовою Борщ. Оскільки,
 *   мова не дозволяє обчислення з різними типами даних, потрібно
 *   вручну вказувати до якого типу зводити змінну чи константу.
 */

дійснеЧисло = дійсний(21);             // 21.0

ціле_значення = цілий(19.99);          // 19

рядок_з_числа = рядок(ціле_значення);  // "19"

дійснеЗРядка = дійсний("18.09");       // 18.09

логічне_з_рядка = логічний("істина");  // істина

логічне_з_числа = логічний(0);         // хиба


/*
 *   3. Загальні функції
 */

// Викликати миттєве завершення виконання програми з повідомленням про помилку.
паніка("Текст помилки");

// Отримання змінної середовища.
середовище("HOME");

// Миттєво вийти з програми з кодом виходу.
вихід(1);


/*
 *   4.Допоміжні функції для роботи з контейнерами
 */

// Обчислення довжини об'єкта (застосовується лише до ітерованих об'єктів: рядок, список та словник).
довжина("Текст");

// Додавання елемента до списку.
якийсь_список = [1, 2.3, "4"];
якийсь_список = додати(якийсь_список, ["дочірній", "список"]);

// Вилучення елемента зі словника за ключем.
якийсь_словник = {
    1: 2.2,
    "3": істина,
    7.9: ["тут", 2, "слова"]
};
якийсь_словник = вилучити(якийсь_словник, "3");
