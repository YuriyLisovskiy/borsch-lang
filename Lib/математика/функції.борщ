функція стеля(x: дійсне): ціле
    _х = дійсне(ціле(x));
    якщо(_х < x)
        _х = _х + 1.0;
    кінець;

    повернути ціле(_х);
кінець;

функція підлога(х: дійсне): ціле
    якщо (х < 0)
        повернути ціле(х) - 1;
    кінець;

    повернути ціле(х);
кінець;

функція факторіал(число: ціле): ціле
    якщо (число == 0)
        повернути 1;
    кінець;

    повернути число * факторіал(число - 1);
кінець;

функція степінь(число: ціле, експонента: ціле): ціле
    якщо (експонента == 0)
        повернути 1;
    кінець;

    якщо (експонента < 0)
        повернути 1 / степінь(число, -експонента);
    кінець;

    якщо (експонента % 2 != 0)
        повернути число * степінь(число, експонента - 1);
    кінець;

    якщо (експонента % 2 == 0)
        половина = степінь(число, підлога(експонента / 2));
        повернути половина * половина;
    кінець;
кінець;

функція заокруглити(х: дійсне, н_знаків: ціле): дійсне
    переконатися(н_знаків > 0, "кількість знаків менша або дорівнює нулю");

    степінь = степінь(10, н_знаків);
    х = х * степінь;
    якщо (х >= 0)
        повернути ціле(х + 0.5) / степінь;
    кінець;

    повернути ціле(х - 0.5) / степінь;
кінець;
