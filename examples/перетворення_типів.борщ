/*                  Документація
 *
 *   Перетворення типів відбувається за допомогою вбудованих
 *   функцій, які ніде не реалізовані мовою Борщ. Оскільки,
 *   мова не дозволяє обчислення з різними типами даних, потрібно
 *   вручну вказувати до якого типу зводити змінну чи константу.
 */

дійснеЧисло = дійсне(21);              // 21.0

ціле_значення = ціле(19.99);           // 19

рядок_з_числа = рядок(ціле_значення);  // "19"

дійснеЗРядка = дійсне("18.09");        // 18.09

логічне_з_рядка = логічне("істина");   // істина

логічне_з_числа = логічне(0);          // хиба
