/*                      Документація
 *
 *   Для введення даних потрібно використати вбудовану функцію
 *   ввід(), яка ніде не реалізована мовою Борщ. Функція приймає
 *   рядок, який виведе для повідомлення, що саме потрібно ввести,
 *   і повертає рядок.
 *
 *   Вивід даних відбувається за допомогою вбудованої функції
 *   друк() та друкр() для друку об'єкта без переносу на новий
 *   рядок та з ним відповідно. Друкувати можна вбудовані типи:
 *   ціле, дійсне та рядок.
 *   TODO: Для друку не вбудованих типів необхідно створити метод метод __рядок__().
 */

// Ввід.
ввід("Введіть текст: ");

// Приклад введення значень.
дійсне_число = дійсне(ввід("Введіть дійсне число: "));
ціле_число = ціле(ввід("Введіть ціле число: "));
рядкове_значення = ввід("Введіть текст: ");

// Вивід.
друк("Текст, який");
друкр(" потрібно надрукувати.");
