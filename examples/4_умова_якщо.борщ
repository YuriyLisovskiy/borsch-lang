/*              Документація | Умовний оператор 'якщо'
 *
 *   Використовуючи логічні оператори та оператори порівняння
 *   можна будувати галужені алгоритми за допомогою оператора 'якщо'.
 *   Тут можна використовувати три блоки:
 *      - 'якщо' (обов'язковий): виконається, якщо умова буде істиною;
 *      - 'інакше якщо' (додатковий): виконається, якщо результат
 *        перевірки у попередньому блоці є хибним; можна писати декілька
 *        блоків підряд;
 *      - 'інакше' (додатковий): - виконається, якщо попередні два
 *        результати хибні;
 */

вхідне_число = ціле("Введіть ціле число: ");

якщо((вхідне_число > 5) і (вхідне_число <= 10))
{
    друкр("Виконався блок з умовою 'якщо'.");
}
інакше якщо(вхідне_число - 5)
{
    друкр("Виконався блок з умовою 'інакше якщо'.");
}
інакше
{
    друкр("Не виконалася жодна умова.");
}
